# 視聴用。クラスタ内どのノードでも http://<ノードIP>:30096 で開ける。
apiVersion: v1
kind: Service
metadata:
  name: jellyfin
  namespace: home-recorder
spec:
  type: NodePort
  selector:
    app: jellyfin
  ports:
    - name: http
      port: 8096
      targetPort: http
      nodePort: 30096
