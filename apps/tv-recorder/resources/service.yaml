# 予約 UI。クラスタ内どのノードでも http://<ノードIP>:30080 で開ける。
apiVersion: v1
kind: Service
metadata:
  name: tv-recorder
  namespace: home-recorder
spec:
  type: NodePort
  selector:
    app: tv-recorder
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30080
