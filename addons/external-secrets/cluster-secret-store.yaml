apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: cluster-secret-store
spec:
  provider:
    gcpsm:
      projectID: active-chimera-351913
      auth:
        secretRef:
          secretAccessKeySecretRef:
            name: gcp-sa-key
            namespace: external-secrets
            key: credentials.json
