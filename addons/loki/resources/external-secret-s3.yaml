# MinIO の S3 互換 API 認証情報 (Loki 用)。
# MinIO bootstrap 後 (addons/minio/README 参照)、Vault に以下を事前に登録しておくこと:
#   vault kv put secret/minio/monitoring \
#     access-key=<value> \
#     secret-key=<value>
#
# Loki chart の global.extraEnvFrom でそのまま環境変数として展開されるため、
# キー名は env var として有効な MINIO_ACCESS_KEY / MINIO_SECRET_KEY にする。
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: loki-s3-credentials
  namespace: monitoring
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: cluster-secret-store
    kind: ClusterSecretStore
  target:
    name: loki-s3-credentials
  data:
    - secretKey: MINIO_ACCESS_KEY
      remoteRef:
        key: minio/monitoring
        property: access-key
    - secretKey: MINIO_SECRET_KEY
      remoteRef:
        key: minio/monitoring
        property: secret-key
