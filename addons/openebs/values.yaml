## OpenEBS custom values

localpv-provisioner:
  enabled: true

# Chart 4.4.0 uses engines.* keys for sub-chart conditions
engines:
  local:
    lvm:
      enabled: true
    zfs:
      enabled: false
  replicated:
    mayastor:
      enabled: false

loki:
  enabled: false

alloy:
  enabled: false
